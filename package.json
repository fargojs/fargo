{
  "name": "@zotera/monorepo",
  "private": true,
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "format": "prettier --write --cache .",
    "lint": "eslint --cache {packages,vscode}/*/src/**/*",
    "build": "pnpm -r --filter=@zotera/* run build",
    "dev": "pnpm -r --parallel --filter=@zotera/* run dev",
    "test": "pnpm -r --parallel --filter=@zotera/* run test",
    "update:dep-versions": "tsx scripts/update-versions.ts",
    "build:configuration-schema": "typeschema"
  },
  "license": "MIT",
  "devDependencies": {
    "@luxass/eslint-config": "^1.0.8",
    "@luxass/prettier-config": "^1.0.4",
    "@types/node": "^18.11.9",
    "eslint": "^8.27.0",
    "lint-staged": "^13.0.3",
    "node-fetch": "^3.3.0",
    "prettier": "^2.7.1",
    "simple-git-hooks": "^2.8.1",
    "tsup": "^6.4.0",
    "tsutils": "^3.21.0",
    "tsx": "^3.12.1",
    "typeschema": "file:../typeschema",
    "typescript": "^4.8.4",
    "vitest": "^0.25.1",
    "zod": "^3.19.1"
  },
  "engines": {
    "node": ">=16"
  },
  "packageManager": "pnpm@7.5.0",
  "pnpm": {
    "patchedDependencies": {
      "tsup@6.4.0": "patches/tsup@6.4.0.patch",
      "vite@3.2.3": "patches/vite@3.2.3.patch"
    }
  },
  "simple-git-hooks": {
    "pre-commit": "pnpm exec lint-staged --concurrent false"
  },
  "lint-staged": {
    "*": [
      "prettier --write --cache --ignore-unknown"
    ],
    "packages/*/src/**/*.ts": [
      "eslint --cache --fix"
    ],
    "packages/types/src/**/*.ts": [
      "pnpm run build:configuration-schema"
    ]
  }
}
